export class Table {
    constructor(data, columnNames, options) {
        this.data = data;
        this.columnNames = columnNames;
        this.options = options;

        this.mountTable();
    }

    mountTable() {
        
    }
}